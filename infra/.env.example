# PostgreSQL Configuration
POSTGRES_USER=dryft
POSTGRES_PASSWORD=change_this_secure_password_in_production
POSTGRES_DB=dryft

# Redis Configuration
REDIS_PASSWORD=change_this_secure_redis_password_in_production

# Notes:
# 1. Copy this file to .env: cp .env.example .env
# 2. Replace all passwords with strong, unique values
# 3. Use a password generator for production environments
# 4. Never commit the actual .env file to version control
