# Database Connection
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# For Docker: use service name 'postgres' as host
# For local dev: use 'localhost'
DATABASE_URL=postgresql://dryft:change_this_password@postgres:5432/dryft

# Redis Connection
# Format: redis://:PASSWORD@HOST:PORT
# For Docker: use service name 'redis' as host
# For local dev: use 'localhost'
REDIS_URL=redis://:change_this_redis_password@redis:6379

# Worker Configuration
# Number of concurrent jobs to process (1-10)
# Lower = safer, Higher = faster (but more memory/CPU)
WORKER_CONCURRENCY=3

# Notes:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update DATABASE_URL password to match infra/.env POSTGRES_PASSWORD
# 3. Update REDIS_URL password to match infra/.env REDIS_PASSWORD
# 4. For Docker: use service names (postgres, redis)
# 5. For local dev: use localhost instead of service names
# 6. Start with low concurrency (1-3) and increase based on load
