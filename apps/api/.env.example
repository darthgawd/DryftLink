# Environment
NODE_ENV=development

# Server Configuration
API_PORT=3002

# Database Connection
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# For Docker: use service name 'postgres' as host
# For local dev: use 'localhost'
DATABASE_URL=postgresql://dryft:change_this_password@postgres:5432/dryft

# Redis Connection
# Format: redis://:PASSWORD@HOST:PORT
# For Docker: use service name 'redis' as host
# For local dev: use 'localhost'
REDIS_URL=redis://:change_this_redis_password@redis:6379

# JWT Secret
# IMPORTANT: Must be at least 32 characters long
# Generate with: openssl rand -base64 32
JWT_SECRET=change-this-to-a-secure-random-string-min-32-chars-required

# CORS Configuration
# Comma-separated list of allowed origins
# Example: http://localhost:3000,https://yourdomain.com
ALLOWED_ORIGINS=http://localhost:3000

# Notes:
# 1. Copy this file to .env: cp .env.example .env
# 2. Update DATABASE_URL password to match infra/.env POSTGRES_PASSWORD
# 3. Update REDIS_URL password to match infra/.env REDIS_PASSWORD
# 4. Generate a strong JWT_SECRET (min 32 characters)
# 5. For Docker: use service names (postgres, redis)
# 6. For local dev: use localhost instead of service names
